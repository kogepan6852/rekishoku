<%= form_for(@period) do |f| %>
  <% if @period.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@period.errors.count, "error") %> prohibited this period from being saved:</h2>

      <ul>
      <% @period.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


<table class="table table-hover">
  <thead>
    <tr>
      <th>時代名</th>
      <th>詳細</th>
      <th>戻る</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
      <tr>
        <td><%= f.text_field :name ,:class => "form-control" %></td>
        <td><%= link_to '詳細', @period ,:class => "btn btn-success" %></td>
        <td><%= link_to '戻る', periods_path ,:class => "btn btn-primary" %></td>
      </tr>
  </tbody>
</table>

<div class="center">
  <%= f.submit :class=>"btn" %>
</div>
<% end %>
</div>
