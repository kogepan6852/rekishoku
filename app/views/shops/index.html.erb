    <!-- パソコン表示用 -->
    <div class="hidden-xs">
      <!-- タイトル -->
      <h1 class="title-position"><i class="fa fa-home"></i>SHOPトップ</h1>
      <!-- 店舗登録 -->
      <div class="text-right">
        <%= link_to "<i class='fa fa-plus'></i>SHOP登録".html_safe, new_shop_path, :class => "btn btn-default btn-round-lg btn-lg new-padding" %>
      </div>
      <!-- 本文 -->
      <table class="table">
          <tr>
            <td>名前</td>
            <td>説明</td>
            <td>都道府県</td>
            <td>市区町村</td>
            <td>その他住所</td>
            <td class="text-right">詳細</td>
            <td class="text-right">変更</td>
            <td class="text-right">削除</td>
          </tr>
        <% @shops.each do |shop| %>
          <tr>
            <td><%= shop.name %></td>
            <td><%= truncate(shop.description, length: 60) %></td>
            <td><%= shop.province %></td>
            <td><%= shop.city %></td>
            <td><%= shop.address1 %></td>
            <td class="text-right fontawesome-icon"><%= link_to "<i class='fa fa-file-text-o fa-3x'></i>".html_safe, shop %></td>
            <td class="text-right fontawesome-icon"><%= link_to "<i class='fa fa-pencil fa-3x'></i>".html_safe, edit_shop_path(shop) %></td>
            <td class="text-right fontawesome-icon"><%= link_to "<i class='fa fa-trash fa-3x'></i>".html_safe, shop, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </table>
      <!-- ページネーション -->
      <%= paginate(@shops) %>
    </div>

    <!-- スマホ表示用 -->
    <div class="visible-xs">
      <!-- タイトル -->
      <h1 class="text-center"><i class="fa fa-home"></i>SHOPトップ</h1>
      <hr>
      <!-- 店舗登録 -->
      <div class="text-right">
        <%= link_to "<i class='fa fa-plus'></i>SHOP登録".html_safe, new_shop_path, :class => "btn btn-default btn-round-lg btn-lg new-padding" %>
      </div>
      <!-- 本文 -->
      <table class="table table-hover">
          <tr>
            <td>名前</td>
            <td class="text-right">詳細</td>
            <td class="text-right">変更</td>
            <td class="text-right">削除</td>
            <th colspan="3"></td>
          </tr>
        <% @shops.each do |shop| %>
          <tr>
            <td><%= shop.name %></td>
            <td class="text-right fontawesome-icon"><%= link_to "<i class='fa fa-file-text-o fa-3x'></i>".html_safe, shop %></td>
            <td class="text-right fontawesome-icon"><%= link_to "<i class='fa fa-pencil fa-3x'></i>".html_safe, edit_shop_path(shop) %></td>
            <td class="text-right fontawesome-icon"><%= link_to "<i class='fa fa-trash fa-3x'></i>".html_safe, shop, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          </tr>
        <% end %>
      </table>
      <!-- ページネーション -->
      <%= paginate(@shops) %>
    </div>
